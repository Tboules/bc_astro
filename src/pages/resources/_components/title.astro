---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class='mb-8 flex gap-6 items-center'>
  <h1 class='text-primary text-2xl md:text-3xl font-medium whitespace-nowrap'>{title}</h1>
  <div class='w-full h-0.5 bg-primary' />
</div>
